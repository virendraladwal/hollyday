<%= form_with(model: kamal) do |form| %>
  <% if kamal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(kamal.errors.count, "error") %> prohibited this kamal from being saved:</h2>

      <ul>
        <% kamal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :index, style: "display: block" %>
    <%= form.text_field :index %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
